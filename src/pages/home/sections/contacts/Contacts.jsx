import styles from "./contacts.module.css";

function Contacts() {
  return (
    <div className={`${styles.contacts} home-section`} id="contacts">
      <h2 className="hidden">Contacts</h2>
    </div>
  );
}

export default Contacts;
